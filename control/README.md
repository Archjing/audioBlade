# Control

Future profile, routing-state, diagnostics, and lifecycle control code lives here.

The Rust control service is deferred until the MVP audio path and profile model justify a separate non-real-time service.
