# Phase 0 Week 2 Device Enumeration Sample

## Status

Result: `not_run`

Reason:

Real Windows audio-device enumeration evidence has not yet been captured from a supported host environment.

## Evidence Template

Date:

Host OS:

Environment label:

Audio backend or API:

Command:

```bash
./build/apps/desktop/audioBlade --diagnose-devices
```

Default input:

Default output:

Visible devices:

```text
Replace this block with the captured diagnostics output.
```

## Notes

- Save one filled-in sample from a real Windows development environment.
- Keep platform-specific fields if available, but preserve the minimal cross-platform field set:
  - device name
  - direction
  - channel count
  - default status
  - sample rate or format capability when available
